apiVersion: v1
kind: Service
metadata:
  name: mongodb-metrics-app
  namespace: observability
  labels:
    app: mongodb-metrics-app
spec:
  selector:
    app: mongodb-metrics-app
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
